<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.mapper.ChapterMapper">

    <resultMap id="BaseResultMap" type="com.training.entity.Chapter">
        <id column="id" property="id"/>
        <result column="course_id" property="courseId"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, course_id, title, description, sort_order, created_at, updated_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM chapter WHERE id = #{id}
    </select>

    <select id="selectByCourseId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM chapter WHERE course_id = #{courseId} ORDER BY sort_order ASC
    </select>

    <insert id="insert">
        INSERT INTO chapter (course_id, title, description, sort_order, created_at, updated_at)
        VALUES (#{courseId}, #{title}, #{description}, #{sortOrder}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateById">
        UPDATE chapter
        <set>
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM chapter WHERE id = #{id}
    </delete>

    <delete id="deleteByCourseId">
        DELETE FROM chapter WHERE course_id = #{courseId}
    </delete>

    <select id="countByCourseId" resultType="int">
        SELECT COUNT(*) FROM chapter WHERE course_id = #{courseId}
    </select>

</mapper>
