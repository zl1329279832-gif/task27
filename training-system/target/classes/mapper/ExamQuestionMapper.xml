<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.training.mapper.ExamQuestionMapper">

    <resultMap id="BaseResultMap" type="com.training.entity.ExamQuestion">
        <id column="id" property="id"/>
        <result column="exam_id" property="examId"/>
        <result column="question_id" property="questionId"/>
        <result column="question_snapshot" property="questionSnapshot"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="score" property="score"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, exam_id, question_id, question_snapshot, sort_order, score
    </sql>

    <select id="selectByExamId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM exam_question
        WHERE exam_id = #{examId}
        ORDER BY sort_order
    </select>

    <insert id="insert" parameterType="com.training.entity.ExamQuestion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO exam_question (exam_id, question_id, question_snapshot, sort_order, score)
        VALUES (#{examId}, #{questionId}, #{questionSnapshot}, #{sortOrder}, #{score})
    </insert>

    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO exam_question (exam_id, question_id, question_snapshot, sort_order, score)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.examId}, #{item.questionId}, #{item.questionSnapshot}, #{item.sortOrder}, #{item.score})
        </foreach>
    </insert>

    <delete id="deleteByExamId">
        DELETE FROM exam_question WHERE exam_id = #{examId}
    </delete>

    <select id="countByExamId" resultType="int">
        SELECT COUNT(*) FROM exam_question WHERE exam_id = #{examId}
    </select>

</mapper>
